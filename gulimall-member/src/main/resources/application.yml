server:
  port: 8000

spring:
  datasource:
    url: jdbc:mysql://47.104.204.228:3306/gulimall_ums
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
    #使用8.0以上的mysql驱动，驱动名要加cj
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
    sentinel:
      transport:
        dashboard: 127.0.0.1:8080
        port: 8719
  application:
    name: gulimall-member
  thymeleaf:
    cache: false
  session:
    store-type: redis
  redis:
    host: 47.104.204.228
    port: 6379

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  #mapper-locations: classpath:/mapper/**/*.xml
  #classpath之后带*表示扫描当前项目以及jar包里面的classpath
  #不带*表示只扫描当前项目下的classpath
  global-config:
    db-config:
      id-type: auto

management:
  endpoints:
    web:
      exposure:
        include: '*'
feign:
  sentinel:
    enabled: true
